<div class="card-body">
    <%= bootstrap_form_with(model: customertype, local: true, layout: :horizontal) do |form| %>
    <% if customertype.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(customertype.errors.count, "error") %> prohibited this customertype from being saved:</h2>

        <ul>
            <% customertype.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :customertype, label: "Customer Type", minlength: 2, maxlength: 30, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>