<div class="card-body">
    <%= bootstrap_form_with(model: customertype, local: true, layout: :vertical) do |form| %>
    <!--<% if customertype.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(customertype.errors.count, "error") %> prohibited this customertype from being saved:</h2>

        <ul>
            <% customertype.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@customertype) %>

    <div class="field">
        <%= form.text_field :customertype, label: "Customer Type", maxlength: 128, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>