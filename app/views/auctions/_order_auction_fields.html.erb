<%# Nested Form Partial %>
<div class='nested-fields'>

    <div class="field">
        <%= f.collection_select :order_id, Order.where("id > 0").order(created_at: :desc), :id, :ordernum, {include_blank: ""}, required: true %>
    </div>

    <%= link_to_remove_association "Remove Order", f %>

</div>
<%# End of Nested Form Partial %>