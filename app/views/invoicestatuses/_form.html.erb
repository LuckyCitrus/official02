<div class="card-body">
    <%= bootstrap_form_with(model: invoicestatus, local: true, layout: :horizontal) do |form| %>
    <!--<% if invoicestatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(invoicestatus.errors.count, "error") %> prohibited this invoicestatus from being saved:</h2>

        <ul>
            <% invoicestatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@invoicestatus) %>

    <div class="field">
        <%= form.text_field :invoicestatus, label: "Invoice Status", maxlength: 128, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>