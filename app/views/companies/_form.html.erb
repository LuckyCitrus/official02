<div class="card-body">
    <%= bootstrap_form_with(model: company, local: true, layout: :horizontal) do |form| %>
    <!--<% if company.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

        <ul>
            <% company.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>-->

    <div class="field">
        <%= form.text_field :companyname, label: "Company Name", maxlength: 128, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>