<div class="card-body">
    <%= bootstrap_form_with(model: employeestatus, local: true, layout: :horizontal) do |form| %>
    <% if employeestatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(employeestatus.errors.count, "error") %> prohibited this employeestatus from being saved:</h2>

        <ul>
            <% employeestatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :employeestatus, label: "Employee Status", minlength: 2, maxlength: 30, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>