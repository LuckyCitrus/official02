<div class="card-body">
    <%= bootstrap_form_with(model: invoiceshipment, local: true, layout: :vertical) do |form| %>
    <!--<% if invoiceshipment.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(invoiceshipment.errors.count, "error") %> prohibited this invoiceshipment from being saved:</h2>

        <ul>
            <% invoiceshipment.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@invoiceshipment) %>

    <div class="field">
        <%= form.collection_select :invoice_id, Invoice.where("id > 0").order(created_at: :desc), :id, :invoice_info, {include_blank: ""}, required: true %>
    </div>

    <div class="field">
        <%= form.collection_select :shipment_id, Shipment.where("id > 0").order(created_at: :desc), :id, :shipmentnum, {include_blank: ""}, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>