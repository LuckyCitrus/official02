<div class="card-body">
    <%= bootstrap_form_with(model: department, local: true, layout: :vertical) do |form| %>
    <!--<% if department.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(department.errors.count, "error") %> prohibited this department from being saved:</h2>

        <ul>
            <% department.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@department) %>

    <div class="field">
        <%= form.text_field :departmentname, label: "Department Name", maxlength: 128, required: true %>
    </div>

    <div class="field">
        <%= form.collection_select :company_id, Company.order(:companyname).all, :id, :companyname, {include_blank: ""}, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>