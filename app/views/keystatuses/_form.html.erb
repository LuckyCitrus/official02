<div class="card-body">
    <%= bootstrap_form_with(model: keystatus, local: true, layout: :horizontal) do |form| %>
    <% if keystatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(keystatus.errors.count, "error") %> prohibited this keystatus from being saved:</h2>

        <ul>
            <% keystatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :keystatus, label: "Key Status", minlength: 2, maxlength: 30, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>