<div class="card-body">
    <%= form_with(model: keystatus, local: true) do |form| %>
    <% if keystatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(keystatus.errors.count, "error") %> prohibited this keystatus from being saved:</h2>

        <ul>
            <% keystatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.label :keystatus, "Key Status" %>
        <%= form.text_field :keystatus, minlength: 2, maxlength: 30, size: 24, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>