<div class="card-body">
    <%= bootstrap_form_with(model: titlestatus, local: true, layout: :horizontal) do |form| %>
    <% if titlestatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(titlestatus.errors.count, "error") %> prohibited this titlestatus from being saved:</h2>

        <ul>
            <% titlestatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :titlestatus, label: "Title Status", minlength: 2, maxlength: 30, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>