<%= form_with(model: order_auction, local: true) do |form| %>
  <% if order_auction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_auction.errors.count, "error") %> prohibited this order_auction from being saved:</h2>

      <ul>
        <% order_auction.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :auction_id %>
    <%= form.text_field :auction_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
