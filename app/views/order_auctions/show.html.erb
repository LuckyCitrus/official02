<div class="row row-fluid">
    <div class="col-sm-6">
        <div class="card">
            <div class="card-body">
                <p>
                    <strong>Order:</strong>
                    <%= @order_auction.order.ordernum %>
                </p>
                <p>
                    <strong>Auction:</strong>
                    <%= @order_auction.auction.auctionname %>
                </p>
                <%= link_to 'Edit', edit_order_auction_path(@order_auction), class: "btn btn-primary" %>
                <%= link_to 'Back', order_auctions_path, class: "btn btn-secondary" %>
            </div>
        </div>
    </div>
</div>