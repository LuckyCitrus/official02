<div class="card-body">
    <%= bootstrap_form_with(model: country, local: true, layout: :vertical) do |form| %>
    <!--<% if country.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(country.errors.count, "error") %> prohibited this country from being saved:</h2>

        <ul>
            <% country.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@country) %>

    <div class="field">
        <%= form.text_field :country_name, label: "Country Name", maxlength: 128, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>