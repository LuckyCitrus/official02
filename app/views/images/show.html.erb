<!--<h2>Image</h2>

<div class="row">
    <div class="col-md-9">
        <div class="card">
            <div class="card-body p-2">
                <%= image_tag @image.image, class: 'd-block w-100' %>
            </div>
        </div>
    </div>

    <div class="col-md-3">
        <%= link_to @image.user.email, users_path(@image.user.email) %>

        <p class="border-top mt-3 py-2">
            <% if @image.description.present? %>
            <%= @image.description %> -
            <% end %>
            <span class="text-muted"><%= time_ago_in_words @image.created_at %> ago</span>
        </p>
    </div>
</div>-->

<div class="row row-fluid">
    <div class="col-md-4">
        <div class="card">
            <div class="view overlay">
                <%= image_tag @image.image, class:"card-img-top img-fluid" %>
                <div class="mask rgba-white-slight"></div>
                </a>
            </div>

            <div class="card-body">

                <h4 class="card-title">Order # <%= @image.order.ordernum%></h4>
                <% if @image.description.present? %>
                <p class="card-text"><%= @image.description %></p>
                <% end %>
                <p class="border-top mt-3 py-2">
                    Posted by <%= link_to @image.user.email, @image.user %> - 
                    <span class="text-muted"><%= time_ago_in_words @image.created_at %> ago</span>
                </p>
            </div>
            <div class="card-footer">
                <%= link_to 'Edit', edit_image_path(@image), class: "btn btn-primary" %>
                <%= link_to 'Back', images_path, class: "btn btn-secondary" %>
            </div>
        </div>
    </div>
</div>