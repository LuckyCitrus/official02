<div class="card-body">
    <%= bootstrap_form_with(model: customer, local: true, layout: :horizontal) do |form| %>
    <% if customer.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(customer.errors.count, "error") %> prohibited this customer from being saved:</h2>

        <ul>
            <% customer.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :first_name, label: "First Name", pattern: "^[a-zA-Z-']*$", minlength: 2, maxlength: 36, required: true %>
    </div>

    <div class="field">
        <%= form.text_field :last_name, label: "Last Name", pattern: "^[a-zA-Z-']*$", minlength: 2, maxlength: 36, size: 24, required: true %>
    </div>

    <div class="field">
        <%= form.text_field :address, pattern: "^[#'.0-9a-zA-Z\s,-]*$", minlength: 5, maxlength: 72, size: 24, required: true %>
    </div>

    <div class="field">
        <%= form.telephone_field :phone, pattern: '[^a-zA-Z]*', minlength: 5, maxlength: 24, required: true %>
    </div>

    <div class="field">
        <%= form.email_field :email, minlength: 5, maxlength: 254, size: 24, required: true %>
    </div>
    <!--, pattern: "[^@]+@[^@]+"-->

    <div class="field">
        <%= form.collection_select :user_id, User.order(:email), :id, :email, {include_blank: ""} %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>