<div class="card-body">
    <%= bootstrap_form_with(model: paymentstatus, local: true, layout: :vertical) do |form| %>
    <!--<% if paymentstatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(paymentstatus.errors.count, "error") %> prohibited this paymentstatus from being saved:</h2>

        <ul>
            <% paymentstatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>--><%= errors_for(@paymentstatus) %>

    <div class="field">
        <%= form.text_field :paymentstatus, label: "Payment Status", maxlength: 128, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>