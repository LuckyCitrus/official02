<%= form_with(model: customerstatus, local: true) do |form| %>
  <% if customerstatus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customerstatus.errors.count, "error") %> prohibited this customerstatus from being saved:</h2>

      <ul>
        <% customerstatus.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customerstatus %>
    <%= form.text_field :customerstatus %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
