<div class="card-body">
    <%= bootstrap_form_with(model: customerstatus, local: true, layout: :horizontal) do |form| %>
    <% if customerstatus.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(customerstatus.errors.count, "error") %> prohibited this customerstatus from being saved:</h2>

        <ul>
            <% customerstatus.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
    </div>
    <% end %>

    <div class="field">
        <%= form.text_field :customerstatus, label: "Customer Status", minlength: 2, maxlength: 30, required: true %>
    </div>

    <div class="actions">
        <%= form.submit :Submit, class: 'btn btn-success' %>
    </div>
    <% end %>
</div>